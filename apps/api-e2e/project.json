{
  "name": "api-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/api-e2e/src",
  "tags": [],
  "implicitDependencies": ["api"],
  "// targets": "the `test` (jest) and `lint` (eslint) targets are inferred from jest.config.ts + eslint.config.mjs",
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "command": "jest --config apps/api-e2e/jest.config.ts --runInBand"
      }
    }
  }
}
